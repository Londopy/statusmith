{
  "name": "statusmith",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --no-bundle",
    "bundle": "tauri build --bundles nsis",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
