{
  "name": "statusmith",
  "private": true,
  "version": "0.4.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --no-bundle",
    "bundle": "tauri build --bundles nsis",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
